services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-cadastro}
    volumes:
      - pgdata:/var/lib/postgresql/data
    # Porta do Postgres publicada no host, para acessar o banco por um cliente
    # externo (Beekeeper, DBeaver, psql). A porta interna do container continua
    # 5432 (usada pela API na rede do Compose); só a porta do host é configurável.
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: ./backend
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-cadastro}?schema=public
      # Porta em que a API escuta. Configurável via API_PORT (o Nginx e o host
      # abaixo usam a mesma variável, então mudar aqui não quebra o proxy).
      PORT: ${API_PORT:-3001}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:8080}
      NODE_ENV: production
    ports:
      - "${API_PORT:-3001}:${API_PORT:-3001}"
    depends_on:
      db:
        condition: service_healthy

  web:
    build: ./frontend
    environment:
      # Repassada ao template do Nginx para montar o proxy_pass para a API.
      API_PORT: ${API_PORT:-3001}
    ports:
      - "${WEB_PORT:-8080}:80"
    depends_on:
      - api

volumes:
  pgdata:
